# V0.3 Validation / V0.3 验证记录

Last verified: 2026-08-10

## 中文

| 检查 | 状态 | 证据边界 |
| --- | --- | --- |
| 双语教程与本地链接 | PASS | 仓库静态检查 |
| Agent Lab 基线 | PASS | Python 标准库，2 tests |
| 准备脚本 | PASS | Claude/Codex 独立临时 Git 副本 |
| Codex Agent Runtime | PARTIAL / USER TERMINAL REQUIRED | 已连接官方 `openai` Provider 与 `gpt-5.6-sol`；嵌套 Codex 环境强制只读，没有文件改动 |
| Claude Code Runtime | DOCUMENTATION ONLY | 未提供 Anthropic 官方凭据；兼容路线需用户临时输入合法 Key |
| 旧 Open WebUI loopback 迁移 | INDEPENDENT SECURITY DEBT | 不属于 V0.3/V0.4 数据路径；不阻塞 RC，本次 Runtime 禁止改动 |

不记录登录身份、邮箱、Key、Token、完整用户路径、提示全文或账户页面。Runtime 成功必须同时满足测试通过、作用域检查通过和人工 `git diff` 复核。

### Codex 嵌套运行证据

- CLI：`codex-cli 0.147.0`
- Provider：`openai`
- 客户端报告模型：`gpt-5.6-sol`
- 请求沙箱：`workspace-write`
- Codex 桌面任务内的实际沙箱：`read-only`
- 结果：编辑被拒绝；`git diff -- src tests` 为空；没有安装、工具联网、提交、推送、Open WebUI 或 Docker 访问
- 后续：在普通 Windows PowerShell 中运行教程命令，再执行 `verify-agent-lab.ps1 -Agent codex`

## English

Static bilingual docs, links, the two-test baseline, and isolated workspace preparation pass. Codex reached the official provider, but this nested Codex desktop environment enforced read-only and safely rejected edits; a normal user-terminal run is required for write-runtime PASS. Codex runtime is independent of Open WebUI. The legacy port-3000 instance remains separate security debt and is explicitly excluded from V0.3/V0.4 runtime. Claude Code is documentation-only without an official Anthropic credential. Evidence excludes identity, credentials, full user paths, prompts, and account pages.
